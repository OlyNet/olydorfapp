<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">

    </style>

    <color name="actionbar_text">#000000</color>
    <color name="actionbar_background">#FCC90E</color>
    <color name="actionbar_tab_background">#000000</color>
    <color name="actionbar_tab_text">#FFFFFF</color>

    <style name="OlyNetTheme"
        parent="@style/Theme.AppCompat.Light">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <!-- <item name="android:actionBarTabTextStyle">@style/TabTextStyle</item> -->
        <item name="android:actionMenuTextColor">@color/actionbar_text</item>
        <item name="android:actionBarTabStyle">@style/TabBarStyle</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBar</item>
        <!-- <item name="actionBarTabTextStyle">@style/TabTextStyle</item> -->
        <item name="actionMenuTextColor">@color/actionbar_text</item>
        <item name="actionBarTabStyle">@style/TabBarStyle</item>
    </style>

    <style name="TabBarStyle"
        parent="@style/Widget.AppCompat.ActionBar">
        <!-- <item name="android:background">@color/actionbar_tab_background</item>
        <item name="android:backgroundStacked">@color/actionbar_tab_background</item>
        <item name="android:backgroundSplit">@color/actionbar_tab_background</item>


        <item name="background">@color/actionbar_tab_background</item>
        <item name="backgroundStacked">@color/actionbar_tab_background</item>
        <item name="backgroundSplit">@color/actionbar_tab_background</item> -->
    </style>

    <!-- general styles for the action bar -->
    <style name="MyActionBar"
        parent="@style/Widget.AppCompat.ActionBar">
        <item name="android:titleTextStyle">@style/TitleTextStyle</item>
        <item name="android:background">@color/actionbar_background</item>
        <item name="android:backgroundStacked">@color/actionbar_background</item>
        <item name="android:backgroundSplit">@color/actionbar_background</item>

        <!-- Support library compatibility -->
        <item name="titleTextStyle">@style/TitleTextStyle</item>
        <item name="background">@color/actionbar_background</item>
        <item name="backgroundStacked">@color/actionbar_background</item>
        <item name="backgroundSplit">@color/actionbar_background</item>
    </style>

    <!-- action bar title text -->
    <style name="TitleTextStyle"
        parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/actionbar_text</item>
    </style>

    <!-- action bar tab text -->
    <style name="TabTextStyle"
        parent="@style/Widget.AppCompat.ActionBar.TabText">
    </style>

</resources>
